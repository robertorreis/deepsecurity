- name: Deep Security Agentes Windows
  include_vars:
    file: windows.yml

- name: Copy script file
  win_copy:
    src: "{{ windows_script }}"
    dest: "{{ default }}"
    remote_src: no

- name: Run remote install
  win_command: "{{ windows_install }}"
